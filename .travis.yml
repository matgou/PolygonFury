language: c
compiler: gcc

before_install:
    - sudo apt-get update -qq
    - sudo apt-get install -qq -y libsdl-console-dev libsdl-ttf2.0-dev libsdl2-dev

script: ./build.sh

notifications:
        email: false
